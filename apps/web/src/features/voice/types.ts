/**
 * Feature-facing types for `voice`.
 *
 * Nothing outside this file imports from `generated/` — regenerating the
 * contract then ripples through one module rather than the whole app.
 *
 * This feature has no wire types yet. When it gains an endpoint, re-export the
 * generated schema here and add the compile-time check that the request body
 * still matches the path, as `features/kundali/types.ts` does.
 */

export {};
